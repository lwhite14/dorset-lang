fn binary : 1 (x y) { y; }

extern externfunction(x y);

fn fibi(x)
{
    var a = 1, b = 1, c in
    (for i = 3, i < x in
        c = a + b :
        a = b :
        b = c) :
    b;
}

fn main() 
{ 
    printf("%f", fibi(10)) : 
    printf("\n", 0);
}