fn binary>10(LHS, RHS) double {
    return RHS < LHS;
}

fn newLine() void {
    printf("\n", 0);
}

fn test() double {
    for (i = 1, i < 10, 1.0)
    {
        if (i > 5)
        {
            printf("Larger than 5: %f", i);
            newLine();
            return i;
        }
        else
        {
            0;
        }
    }
    return 0;
}

fn main() void {
    var x = test();
    printf("Final number is: %f", x);
    newLine();
}