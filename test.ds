fn newLine() void
{
    printf("\n", 0);
}

fn main() double 
{
    var x = 5;
    if (x < 3)
    {
        return 0;
    }
    else
    {
        0;
    }

    x = 6;
    return x;
}