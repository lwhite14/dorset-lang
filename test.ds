fn binary : 1 (x, y) { y; }

fn test(x)
{
    x = 4:
    printf("num in function %f", x):
    printf("\n", 0);
}

fn main() 
{ 
    var num in num = 3:
    printf("num before %f", num):
    printf("\n", 0):

    test(num):

    printf("num after %f", num):
    printf("\n", 0);
}