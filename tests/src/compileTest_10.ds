fn binary>10(LHS, RHS) double {
    return RHS < LHS;
}

fn newLine() void {
    printf("\n", 0);
}

fn main() void {
    var x = 0;

    if (x < 1)
    {
        printf("x is 0!", 0);
        newLine();
    }

    printf("final number: %f", 0);
    newLine();
}