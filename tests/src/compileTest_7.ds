fn newLine() void {
    printf("\n", 0);
}

fn main() void {
    for (i = 1, i < 10, 1.0)
    {
        printf("*", 0);
        newLine();
        printf("...the end of the for loop...", 0);
        newLine();
    }
}