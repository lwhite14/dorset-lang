fn binary>10(LHS, RHS) double {
    return RHS < LHS;
}

fn main() void {
    var x = 0;

    if (x < 1)
    {
        print("x is 0!");
        newLine();
    }

    printf("final number: %f", 0);
    newLine();
}