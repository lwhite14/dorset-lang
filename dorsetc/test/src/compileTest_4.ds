fn main() void 
{
    var x = 5;
    if (x < 3)
    {
        print("X is smaller than 3!");
        newLine();
        print("..this code was executed in the then block.");
        newLine();
    }
    else
    {
        print("X is bigger than 3!");
        newLine();
        print("..this code was executed in the else block.");
        newLine();
    }
}